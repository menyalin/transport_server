export const DOWNTIME_TYPES = ['repair', 'holiday']

export enum POINT_TYPES_ENUM {
  loading = 'loading',
  unloading = 'unloading',
}
export const POINT_TYPE_VALUES = Object.values(POINT_TYPES_ENUM)
